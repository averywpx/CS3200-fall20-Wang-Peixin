const FIND_ALL_MEETINGS  = "http://localhost:8080/findAllMeetings"

const findAllMeetings = () =>{
    console.log(FIND_ALL_MEETINGS)
    fetch(`${FIND_ALL_MEETINGS}`)
        .then(response => response.json())
}
